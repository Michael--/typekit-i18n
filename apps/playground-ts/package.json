{
  "name": "@typekit-i18n/playground-ts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "dev": "vite",
    "gen": "echo \"No code generation defined for playground-ts\"",
    "build": "vite build",
    "lint": "pnpm -w exec eslint \"src/**/*.ts\"",
    "preview": "vite preview",
    "test": "pnpm -w exec vitest run --passWithNoTests",
    "typecheck": "pnpm -w exec tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "typekit-i18n": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^6.2.0"
  }
}
